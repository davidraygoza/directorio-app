<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Nombre' %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :numbers,'Telefonos' %><br />
    <%= f.text_field :numbers %>
  </div>
  <div class="field">
    <%= f.label :dependency_id ,'Dependencia'%><br />
     	<%= f.select   :dependency_id,@dependencies %>
   
  </div>
  <div class="field">
    <%= f.label :comments,'Comentarios' %><br />
    <%= f.text_field :comments %>
  </div>
  <div class="actions">
      <%= f.submit 'Guardar',:class=>'btn  btn-info' %>
  </div>
<% end %>
